# Secrets - never commit real keys
.streamlit/secrets.toml
.env
*.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local runtime data (SQLite DB, per-user memory files) - these are
# per-deployment state, not source, and contain user data
data/
user_memory/

# OS / editor cruft
.DS_Store
.vscode/
.idea/
